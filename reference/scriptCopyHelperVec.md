# copy script files and other from a package to workdir

copy script files and other from a package to workdir

## Usage

``` r
scriptCopyHelperVec(runscripts, workdir = getwd(), packagename = "prolfqua")
```
