% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidyMS_moderation.R
\name{adjust_p_values}
\alias{adjust_p_values}
\title{adjust columns}
\usage{
adjust_p_values(
  contrast_df,
  column = "p.value",
  group_by_col = "contrast",
  newname = "FDR"
)
}
\arguments{
\item{contrast_df}{data.frame with p-values to adjust}

\item{column}{name of column containing p-values}

\item{group_by_col}{column(s) to group by before adjusting (e.g. contrast), or NULL for no grouping}

\item{newname}{name of the new column with adjusted p-values}
}
\value{
The computed result.
}
\description{
adjust columns
}
\examples{

bb <- c(runif(1000), rexp(1500,rate=5))
length(bb)
bb <- bb[bb < 1]
length(bb)
bb <- bb[1:2000]
hist(bb)
data <- data.frame(contrast = rep(LETTERS[1:5],400), p.value = bb)

dataX <- adjust_p_values(data)
Adata <- dataX |> dplyr::filter(contrast == "A")
stopifnot(all.equal(Adata$FDR, p.adjust(Adata$p.value, method="BH")))
data2 <- adjust_p_values(data, group_by_col = NULL)
stopifnot(all.equal(data2$FDR, p.adjust(data2$p.value, method="BH")))


}
