# Bump prolfqua Version to 1.6.2

## Plan

1. Update `DESCRIPTION` from `Version: 1.6.1` to `Version: 1.6.2`.
2. Do not run `make new-version`, because that target also commits, tags, and pushes.
3. Verify the new version from `DESCRIPTION`.
